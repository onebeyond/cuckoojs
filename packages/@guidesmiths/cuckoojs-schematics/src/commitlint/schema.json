{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsCommitlint",
  "title": "commitlint Options Schema",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "commitlint config file destination directory."
    }
  },
  "required": ["directory"]
}