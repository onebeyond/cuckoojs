name: lib - deploy lambdas on environment

on:
  workflow_call:
    inputs:
      env-name:
        required: true
        type: string
#      service-config-vars:
#        required: true
#        type: string
    secrets:
      aws-key:
        required: true
      aws-secret:
        required: true

jobs:
  deploy-serverless-on-env:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: setup node
        uses: actions/setup-node@v3
        with:
          node-version: 14.x

      - name: install deps
        run: npm ci --ignore-scripts

      - name: serverless config credentials
        uses: serverless/github-action@v3.1
        with:
          args: config credentials --provider aws --key ${{ secrets.aws-key }} --secret ${{ secrets.aws-secret }}

      - name: serverless deploy
        uses: serverless/github-action@v3.1
        with:
          args: deploy --stage ${{ inputs.env-name }}
#        env:
#          SERVICE_CONFIG_VARS: ${{ inputs.service-config-vars}}


