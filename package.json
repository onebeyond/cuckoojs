{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/@guidesmiths/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "test": "lerna run test"
  },
  "devDependencies": {
    "conventional-changelog-conventionalcommits": "^7.0.2",
    "lerna": "^7.4.2"
  }
}
